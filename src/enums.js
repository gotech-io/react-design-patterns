const Orientation = Object.freeze({
  HORIZONTAL: 'horizontal',
  VERTICAL: 'vertical',
});

const Size = Object.freeze({
  SMALL: 'small',
  MEDIUM: 'medium',
  LARGE: 'large',
});

export { Orientation, Size };
